{
  "name": "longplay",
  "private": true,
  "version": "1.0.0-alpha.1",
  "type": "module",
  "description": "Turns a Spotify data export into your full listening history. Runs entirely in the browser — no account, no server, no upload.",
  "author": "Emberfig",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "vite": "^7.3.6",
    "vitest": "^4.1.11"
  }
}
